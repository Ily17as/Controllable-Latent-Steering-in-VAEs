# Controllable-Latent-Steering-in-VAEs
Core hypothesis: latent space editing is often piecewise linear. A single global direction v may be insufficient, but a small set of local directions {v_k} can enable predictable edits.
